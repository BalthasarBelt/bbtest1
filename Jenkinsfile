node {
	def app
    stage('Clone repository') {
        checkout scm
    }

    stage('Build image') {
		/* image 'maven:3.8.1-adoptopenjdk-11' */
        /* app = docker.build("balthasarbelt/bbtest1") */
    }
}
